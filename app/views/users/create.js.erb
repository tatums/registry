<% if @user.save %>
  <% session[:user_id] = @user.id %>
  $('.modal-header button').click();
  $("button#checkout-btn").unbind();
<% else %>
  $(".modal-body form").html("<%= escape_javascript(render 'users/form')%>");
<% end %>